<% formularios.errores(flash, error, {
    password_confirm: {
      label: 'Confirmación de contraseña'
    },
    password: {
      label: 'Contraseña'
    },
    email: {
      label: 'Correo electrónico'
    },
  }) %>
<% layout('../layout') -%>
<% block('navbar', partial('_navbar', {usuario_actual: usuario_actual})) %>
<%-partial('_usuariosForm', {
  usuario_actual: usuario_actual,
  usuario: usuario,
  error: error
  }) %>